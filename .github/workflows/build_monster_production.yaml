name: Build, publish and deploy to MonsterASP.NET - Production

on: 
  push:
    branches: [ production ]

jobs:
  build_and_deploy:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x  

    - name: Restore
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Publish
      run: dotnet publish --configuration Release --output ./publish

    - name: Deploy
      uses: rasmusbuchholdt/simply-web-deploy@2.1.0
      with:
        website-name: ${{ secrets.WEBSITE_NAME_PRODUCTION }}
        server-computer-name: ${{ secrets.SERVER_COMPUTER_NAME_PRODUCTION }}
        server-username: ${{ secrets.SERVER_USERNAME_PRODUCTION }}
        server-password: ${{ secrets.SERVER_PASSWORD_PRODUCTION }}
